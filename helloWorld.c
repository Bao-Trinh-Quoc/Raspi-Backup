#include <stdio.h>

void main() {
    printf("hello world");
}

// a little expermients
// learnt new thing, I can actually set up git using personal access token and 
// automate using git remote set-url origin https://<username>:<token>@github.com/<username>/<repository>.git